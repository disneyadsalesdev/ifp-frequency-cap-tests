# 30-minute coworker session (trainer script)

See **[START-HERE.md](./START-HERE.md)** for links to full docs.

## Before the call

- [ ] Send git access to `ifp-frequency-cap-tests` + `ifp-mcp-server`
- [ ] Send IFP-only `mcp.json` block (not your Snowflake config)
- [ ] Ask them to install Cursor from https://cursor.com before the call

## On the call

1. VPN + IFP portal login  
2. Show two folders + `mcp.json`  
3. `pip install` + MCP green in Settings  
4. Open test repo → Ask (Steps 1–3) → Agent (Step 4)  
5. Optional: one forecast with VPN  

Copy-paste prompts: slide **IFP Practice — Copy These Prompts** in `training-guide.pptx` (build with `py docs/build_deck.py`).
